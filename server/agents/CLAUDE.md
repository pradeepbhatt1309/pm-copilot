# Agents Directory

All agents in this directory MUST return valid JSON only.
No conversational text. No markdown. No explanations.
Output must be parseable by JSON.parse() without pre-processing.
Validation-retry: max 3 attempts on malformed JSON.
